import { Status } from '@/common/constants'

export enum EsaviEstado {
  ACTIVO = Status.ACTIVE,
  INACTIVO = Status.INACTIVE,
}

export enum GlobalesEstado {
  ACTIVO = Status.ACTIVE,
  INACTIVO = Status.INACTIVE,
}
